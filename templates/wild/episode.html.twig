{# templates/wild/program.html.twig #}
{% extends 'base.html.twig' %}
{% block title %}{{ program.title }}{% endblock %}
{% block body %}
    <div class="media">
        <img class="align-self-start mr-3" src="{{program.poster}}" alt="{{ program.title }} poster">
        <div class="media-body">
            <h1 class="mt-0 text-center">{{ program.title }}</h1>
            <h2 class="text-center">Categorie : {{ program.category.name }}</h2>
            <h4 class="mb-5">{{ season.description }}</h4>
            <p>{{ episode.title }}</p>
            <div>
                {{ episode.synopsis }}
            </div>
        </div>
        <div>
            <a href="{{ path('show_season', {'id': season.id }) }}">
                Retour à la saison
            </a><br>
            <a href="{{ path('show_program', {'id': program.id }) }}">
                Retour au programme
            </a><br>
            <a href="{{ path('wild_index') }}">
                Retour à l'accueil
            </a>
        </div>
    </div>

{% endblock %}